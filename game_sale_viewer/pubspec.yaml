name: game_sale_viewr
description: "GameDeal Hunter - 멀티 플랫폼 게임 할인 정보 통합 서비스"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

    
  
  # HTTP 클라이언트
  http: ^1.2.0
  
  # Supabase 백엔드
  supabase_flutter: ^2.5.0
  
  # 상태 관리
  provider: ^6.1.1
  
  # 이미지 캐싱
  cached_network_image: ^3.3.1
  
  # URL 런처
  url_launcher: ^6.2.4
  
  # 차트
  fl_chart: ^0.66.2
  
  # 유틸리티
  intl: ^0.19.0

  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true 
  assets:
    - .env
